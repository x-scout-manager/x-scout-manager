# 009 送信キュー作成

## 現状の動作

候補を選択して送信キューを作成する処理が未実装。

## 期待する動作

候補一覧で選択した候補から送信キューを作成できる。

## 操作フロー

1. 候補一覧で候補をチェック
2. 送信キューに追加
3. Cloud Functions `createSendQueue` を呼ぶ
4. 送信キュー画面へ遷移

## UI参考（既存画面）

- `CandidateListPage`
- `SendQueuePage`

## 技術方針

送信可否判定はCloud Functions側で再検証する。

## 対象ファイル（推定）

- `functions/src/**`
- `lib/features/candidates/**`
- `lib/features/send_queue/**`

## 完了条件

- 除外済み/送信済み候補はキュー化されない
- キューとitemsが作成される
- Functions build/testが通る

## レビュー指摘

未記入。

## 備考

最大件数は初期MVPで100件まで。
