# 残タスク一覧

## 初期タスク

- [x] 実装タスク分割
- [x] Gitリポジトリ初期化
- [x] Firestore詳細設計作成
- [x] Cloud Functions詳細設計作成
- [x] Flutter画面詳細設計作成
- [x] X API検証設計作成
- [x] MVP実装計画作成
- [x] Firebaseプロジェクト作成
- [x] Flutter Webプロジェクト作成
- [x] Firebase Authentication連携
- [x] Firestore接続
- [x] Cloud Functions TypeScript環境構築
- [x] X API疎通確認
- [x] X API投稿検索の検証
- [x] X API DM送信可否の検証
- [x] 手動送信支援モードの実装
- [x] 候補抽出・候補一覧の実装
- [x] 送信キューの実装
- [x] 送信履歴の保存
- [x] 除外リスト管理
- [x] 成果証跡管理

## 実装タスク

- [x] 001 所有アカウント確認と環境方針決定
- [x] 002 Firebaseプロジェクト基盤構築
- [x] 003 Flutter Firebase初期化
- [x] 004 認証と管理者権限
- [x] 005 Firestore Rules初期設計
- [x] 006 設定画面とsettings_scout連携
- [x] 007 テンプレート管理
- [x] 017 共通ナビゲーション導線追加
- [x] 008 候補管理のFirestore連携
- [x] 009 送信キュー作成
- [x] 010 手動送信済み登録
- [x] 011 送信履歴画面
- [x] 012 除外管理
- [x] 013 成果証跡管理
- [x] 014 X API検証
- [x] 015 X API候補抽出
- [x] 016 API DM送信
- [x] 018 ダッシュボード集計表示
- [x] 019 送信キュー一覧画面
- [x] 020 成果記録UIの再設計判断
- [x] 021 Cloud Functions Node.js runtime更新
- [x] 022 候補抽出取り消し機能
- [x] 023 Cloud Functions構成整理
- [x] 024 送信キュー明細読み込み修正と削除
- [x] 025 納品前操作性と解除整合性の改善
- [x] 026 送信キュー一覧の完了キュー表示切替

## 追加開発候補

- [ ] プロフィール検索による候補抽出
  - 初期開発では対象外
  - Xユーザーの名前、username、プロフィール文を条件に候補抽出する
  - 015では `sourceTypes` による統合下地のみ用意する

## 推奨実装順

1. 011 送信履歴画面（完了）
   - 010で保存した `send_histories` を画面で確認できるようにする
   - 成果証跡管理の参照元になるため先に固める
2. 019 送信キュー一覧画面（完了）
   - Drawerから既存キューを再開できるようにする
   - 作成直後以外でも手動送信作業を継続できる状態にする
3. 012 除外管理（完了）
   - 誤送信防止に関わるため、X API候補抽出や本格運用前に実装する
   - 候補一覧/詳細から除外し、除外リストで復元できるようにする
4. 018 ダッシュボード集計表示（完了）
   - 候補、送信済み、除外、キュー状況を実データで把握できるようにする
   - 011/012/019のデータ表示が揃った後に実装すると集計項目が安定する
5. 013 成果証跡管理（完了）
   - 送信履歴を参照して成約記録登録できるようにする
   - 契約上の証跡として候補/履歴/対象売上のスナップショットを保存する
6. 014 X API検証
   - X Developer App、OAuth、投稿検索、DM可否を検証する
   - API DM送信をMVP必須にしない判断を維持する
7. 015 X API候補抽出（完了）
   - 014で投稿検索/ユーザー取得が利用可能と判断できた後に実装する
   - 除外/既送信判定を反映して `candidates` に保存する
8. 016 API DM送信（完了）
   - 014でDM送信が利用可能と判断された場合のみ実装または有効化する
   - デバッグXアカウントで管理画面からのAPI DM送信を確認済み
   - アクセストークン期限切れ時はrefresh tokenで更新して送信する
9. 020 成果記録UIの再設計判断（完了）
   - 初期運用では成約記録UIをメイン導線から非表示にする
   - 必要になった場合は、成果報酬計算ではなく成約記録/成果メモとして再設計する
   - API由来のDMイベントIDや投稿IDを保存するかは、016後に運用判断する
10. 021 Cloud Functions Node.js runtime更新（完了）
   - Node.js 20 runtimeが2026-10-30に廃止予定のため、Node.js 22以降へ更新する
   - Functions deploy前に `firebase-functions` 更新影響を確認する
   - 本番運用停止を避けるため、期限前に単独タスクとして実施する
11. 022 候補抽出取り消し機能（完了）
   - 誤ったタグ条件で抽出した場合に、抽出run単位で候補データを戻せるようにする
   - 送信履歴や送信キューに関わった候補は安全のため解除対象からスキップする
12. 023 Cloud Functions構成整理（完了）
   - `functions/src/index.ts` の肥大化を解消し、Functionsを責務別ファイルへ分離する
   - 以後追加するFunctionsは `index.ts` へ処理本体を直接置かない方針を徹底する
13. 024 送信キュー明細読み込み修正と削除（完了）
   - `send_queues/{queueId}/items` のFirestore Rules read許可を追加する
   - 送信キューを論理削除し、一覧から非表示にする
   - 送信履歴や候補データは削除しない
14. 025 納品前操作性と解除整合性の改善（完了）
   - 削除済み送信キューのitemは候補抽出解除の保護対象から外す
   - テンプレートを論理削除できるようにする
   - 送信キュー詳細から一覧へ戻る導線を追加する
15. 026 送信キュー一覧の完了キュー表示切替（完了）
   - 送信キュー一覧では完了済みキューをデフォルト非表示にする
   - 「完了したキューも表示」をONにした場合のみ完了済みキューを表示する
   - 削除済みキューは引き続き非表示にする

## 運用判断が必要な項目

- [ ] 納品先所有アカウントの有無を確認
- [x] 納品先所有アカウントがない場合の案件専用Googleアカウント作成
- [x] 案件用GitHubアカウント作成
- [x] 案件専用GitHub Organization作成
- [x] 案件専用GitHub Repository作成
- [x] ローカルGitへremote origin設定
- [x] GitHub認証設定
- [x] 初回コミットをremoteへpush
- [x] 作業ブランチ `task/001-account-setup` 作成
- [x] Firebase Project ID決定: `x-scout-manager-prod`
- [ ] Googleアカウントの復旧メール移管方針確認
- [ ] Googleアカウントの2FA移管方針確認
- [ ] Google Cloud Billing管理者確認
- [x] Google Cloud Billingアカウントが open であることを確認
- [x] Firebase CLIを案件専用Googleアカウントへログイン切替
- [x] X Developer Appを作成する所有アカウントの決定
- [x] X Developer App作成
- [x] X APIの利用プランとレート制限確認
- [x] DM送信APIをMVPで有効化するかの判断
- [x] 管理者権限を `users/{uid}.role` で管理するか、Custom Claimsで管理するかの最終判断
- [ ] 成果報告の締め日・報告フォーマットの決定
