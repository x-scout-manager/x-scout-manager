# 納品引き継ぎ

## 1. 納品対象

本リポジトリは X Scout Manager の Flutter Web 管理画面、Firebase 設定、Cloud Functions、Firestore Rules/Indexes、設計ドキュメントを含む。

納品時点の主な機能は以下。

- Firebase Authentication による管理者ログイン
- Firestore による候補、テンプレート、設定、送信キュー、送信履歴、除外対象の管理
- X API 投稿検索による候補抽出
- 候補抽出履歴の表示と抽出解除
- 候補一覧から送信キュー作成
- 手動送信支援、送信済み登録、送信履歴保存
- 除外アカウント管理
- ダッシュボード集計表示
- 成約記録の基盤保持

## 2. 初期MVPでの未完了・追加開発扱い

### API DM送信

`016 API DM送信` は未完了。

X APIでDM送信できることは検証済みだが、送信先側ではメッセージリクエストの非表示領域に入る場合があり、通知されない可能性がある。

初期納品では、確認なしの自動一括送信は実装しない。
標準運用は、送信キュー上で本文を確認し、手動送信または送信済み登録を行う形とする。

API DM送信を有効化する場合は、以下を追加開発として扱う。

- 1操作1送信のUI
- 二重送信防止
- X API送信成功/失敗の履歴保存
- レート制限・アカウント制限への注意表示
- API送信を停止できる設定

### プロフィール検索

プロフィール検索による候補抽出は初期開発対象外。

現時点では投稿内容のタグ検索を起点とする。
プロフィール文、表示名、username を条件にした抽出は追加開発として扱う。

### 成約記録UI

成約記録UIは初期運用ではメインナビゲーションから非表示。

`/conversions` の直接URLと基盤は将来用に保持している。
成果報酬率・成果報酬額は本システムでは計算しない。

## 3. リポジトリ構成

主なディレクトリは以下。

```text
lib/
  app/                アプリ設定、DI、ルーティング
  core/               共通エラー、UI、認証、シリアライズ
  features/           機能別MVVM
functions/
  src/                Cloud Functions TypeScript実装
  package.json        Functions依存関係、Node.js runtime
docs/                 要件、設計、検証、引き継ぎ資料
tasks/                タスク記録
task-review/          レビュー指摘記録
firebase.json         Firebase設定
firestore.rules       Firestore Security Rules
firestore.indexes.json Firestore Indexes
```

Cloud Functions は `functions/src/index.ts` に処理本体を置かず、機能別ファイルへ分割している。

## 4. ローカル開発

### 前提

- Flutter SDK
- Node.js 22
- Firebase CLI
- Firebaseプロジェクト `x-scout-manager-prod` へアクセスできるGoogleアカウント

### Flutter

```bash
flutter pub get
flutter run -d chrome
```

### Cloud Functions

```bash
npm --prefix functions install
npm --prefix functions run build
npm --prefix functions run lint
npm --prefix functions run test
```

### 静的解析

```bash
flutter analyze
```

## 5. Firebase

Firebase Project ID:

```text
x-scout-manager-prod
```

Hosting URL:

```text
https://x-scout-manager-prod.web.app
```

デプロイ例:

```bash
firebase deploy --only hosting --project x-scout-manager-prod
firebase deploy --only functions --project x-scout-manager-prod
firebase deploy --only firestore:rules,firestore:indexes --project x-scout-manager-prod
```

## 6. Secret管理

X APIの秘密情報はFlutter WebやGitHubへ置かない。

本番Functionsで使用するSecret:

```text
X_BEARER_TOKEN
```

設定例:

```bash
firebase functions:secrets:set X_BEARER_TOKEN --project x-scout-manager-prod
```

ローカル検証用の `.env.x-api.local` はGit管理対象外。
リポジトリへ含めるのは `.env.x-api.example` のみ。

## 7. X Developer App

X API検証手順は以下を参照。

- `docs/X_API検証手順.md`
- `docs/DETAIL_x_api_verification.md`

OAuthやBearer Tokenの実値はドキュメント、GitHub、チャットへ記載しない。

## 8. Git引き渡し

remote:

```text
git@github-x-scout-manager:x-scout-manager/x-scout-manager.git
```

納品前に行うこと。

1. `.env.x-api.local` がgit対象外であることを確認する
2. `flutter analyze` を通す
3. `npm --prefix functions run build` を通す
4. `npm --prefix functions run lint` を通す
5. `npm --prefix functions run test` を通す
6. タスク単位でコミットする
7. remoteへpushする
8. GitHubアカウント、Firebase、Google Cloud、X Developer Appの所有・復旧情報をクライアント管理へ移す

## 9. 納品時の動作確認項目

- 管理者ログインできる
- タグ設定を保存できる
- X API候補抽出ができる
- 候補抽出解除ができる
- 候補を選択して送信キューを作成できる
- 送信キューから手動送信済み登録できる
- 送信履歴に記録される
- 候補を除外できる
- 除外アカウントを復元できる
- ダッシュボードの件数が表示される

## 10. 渡す資料と渡さない資料

渡す資料:

- ソースコード一式
- `docs/` 配下の要件・設計・検証・引き継ぎ資料
- `.env.x-api.example`
- Firebase/GitHub/X Developer Appの管理権限

渡さない資料:

- `.env.x-api.local`
- APIキー、Bearer Token、Client Secret、OAuth Tokenの平文メモ
- 個人アカウントの復旧情報

`tasks/` と `task-review/` は開発経緯の記録として残してよい。
クライアントに内部レビュー記録を見せたくない場合は、納品前に別途整理する。
