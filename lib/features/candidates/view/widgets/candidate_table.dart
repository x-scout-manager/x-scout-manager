import 'package:flutter/material.dart';

class CandidateTable extends StatelessWidget {
  const CandidateTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('候補データは未取得です'));
  }
}
